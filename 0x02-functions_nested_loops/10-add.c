#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: the first number
 * @z: the second number
 * Return: addition of both
 */
int add(int a, int z)
{
	return (a + z);
}
