#include "main.h"
/**
 * _abs - function that computes the abs value of an int
 * @n: the int in question
 * Return: abs of the int.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
